language: node_js
node_js:
  - "6"
  - "6.1"
  - "5.11"
  - "iojs"
script:
  echo "Deploying!"
install:
  npm install -g firebase-tools
after_success:
  test $TRAVIS_NODE_VERSION = "6" && test $TRAVIS_BRANCH = "master" && firebase deploy --token $FIREBASE_TOKEN
